// ..................................................

const classNameValidate = (val) => {
    return val && (val.replace(/\s/g, '').length);
};

// ..................................................